package behavioral.state.tv;

public class MainTV {
    static void main(String[] args) {
        RemoteControl remote = new RemoteControl();
        remote.powerPress();
        remote.powerPress();
        remote.powerPress();
    }
}
